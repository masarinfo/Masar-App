{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile --prod=false && cd packages/db && npx prisma db push --accept-data-loss && cd ../.. && npx turbo run build --filter=@masar/api"
  },
  "deploy": {
    "startCommand": "cd apps/api && pnpm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
